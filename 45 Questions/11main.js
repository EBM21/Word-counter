"use strict";
const Names = ["Saud", "Naveed", "Hussain", "Abrar"];
const greetingMessage = "Hello, ";
// Print personalized greetings to each person
names.forEach((name) => {
    console.log(`${greetingMessage}${name}!`);
});
